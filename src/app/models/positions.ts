export enum CommonMessagePositions {
	commonDeviceID
}

export enum PresenceMessagePositions {
	presenceSupportedEffects = 1
}

export enum ConfigurationMessagePositions {
	configurationOnState = 1,
	configurationBrightness,
	configurationEffectCode,
	configurationEffectParam0
}
